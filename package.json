{
  "name": "chat365",
  "private": true,
  "workspaces": [
    "widget",
    "site",
    "worker"
  ],
  "scripts": {
    "build:widget": "npm run build -w widget",
    "build:site": "npm run build -w site",
    "build": "npm run build:site",
    "dev:worker": "npm run dev -w worker",
    "dev": "npm run build && npm run dev:worker"
  }
}
