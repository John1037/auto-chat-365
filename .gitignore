# Dependencies
node_modules/

# Build output
widget/dist/

# Secrets -- never commit real keys. supabase/.env.local holds local dev secrets for
# `functions serve`; production secrets are pushed via `supabase secrets set`.
.env
.env.local
.env.*.local
supabase/.env.local

# Supabase CLI local state (regenerated by `supabase start`/`db reset`, not checked in)
supabase/.branches/
supabase/.temp/

# OS/editor cruft
.DS_Store
Thumbs.db
